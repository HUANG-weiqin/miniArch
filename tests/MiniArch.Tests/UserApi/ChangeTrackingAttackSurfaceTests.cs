// All tests deleted in YAGNI cleanup — they referenced Changes() and ModifiedChunks()
// which were removed as part of the change tracking simplification.
// Only Transitions and ValueChanges remain for the new minimal API surface.
